#ifndef VERTEX_H
#define VERTEX_H

struct Vertex {
	/*Before even writing any code, I know that this vertex should have four pointers.
	  Each cardinal direction gets a pointer, treating nullptr as a wall that cannot be
	  surpassed by any type of traversal algorithm.*/
	int room;                                       //The saved room number
	
};

#endif
